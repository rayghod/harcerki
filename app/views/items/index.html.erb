<h1>Listing Items</h1>

  <table>
    <thead>
      <tr>
        <th>Nazwa</th>
        <th>Numer</th>
        <th>Cena</th>
        <th>Okładka</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @item.each do |item| %>
        <%= render "item_row", item: item, order_item: @order_item %>
      <% end %>
    
<!--
    <% @items.each do |item| %>
      <tr>
        <td><%= item.name %></td>
        <td><%= item.number %></td>
        <td><%= item.price %></td>
        <td><%= image_tag(item.photo_url, :width => 100) if item.photo.present? %></td>
        <td><%= link_to 'Show', item %></td>
        <td>
          <% if can? :update, item %>
            <%= link_to 'Edit', edit_item_path(item) %>
          <% end %>
        </td>
        <td>
          <% if can? :destroy, item %>
            <%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' } %>
          <% end %>
        </td>
      </tr>
    <% end %>
 -->

<br>

<% if can? :create, Item %>
  <%= link_to 'New Item', new_item_path %>
<% end %>
